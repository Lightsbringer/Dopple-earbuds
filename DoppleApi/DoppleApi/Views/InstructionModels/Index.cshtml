@model IEnumerable<DoppleApi.Models.InstructionModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImagePath)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Station)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImagePath)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Station.StatusStation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InstructionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InstructionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InstructionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
